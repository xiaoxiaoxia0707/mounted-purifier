#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../.3.0.0@concat/bin/concat" "$@"
  ret=$?
else 
  node  "$basedir/../.3.0.0@concat/bin/concat" "$@"
  ret=$?
fi
exit $ret
